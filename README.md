# LinkTheory
# LinkTheory: Ассоциативная Операционная Система (прототип)

Проект по реализации прикладной модели данных на основе "The Links Theory" ($L \to L^2$). 
Мы отказываемся от иерархических папок и таблиц в пользу единого графа связей.

## Зачем этот репозиторий?
Цель проекта — доказать, что любая информация (файлы, БД, код) может быть эффективно представлена в виде **дуплетов** (связей между двумя ID). 

### Ключевые преимущества:
1. **Глобальная дедупликация**: Одинаковые значения (например, категория "Электроника" в 1000 строк) хранятся как одна точка в памяти.
2. **Многомерность**: Один объект может находиться в бесконечном количестве контекстов (папок/тегов) без копирования данных.
3. **Скорость**: Поиск по ассоциациям работает быстрее, чем традиционные JOIN-запросы в SQL.

## Что мы тестируем (Milestone 1)
Мы берем массив данных (CSV на 1000+ строк) и "размалываем" его в атомарные связи. 

**Задача:** 
Показать, что поиск транзакций по категории через граф связей — это естественная операция системы, а не надстройка над файлом.

## Структура проекта
- `core.py`: Ядро системы. Реализация хранилища дуплетов в SQLite.
- `fs.py`: Слой файловой системы. Эмуляция папок и файлов через связи.
- `importer.py`: Инструмент для импорта CSV-массивов в ассоциативную среду.
- `shell.py`: (В разработке) CLI для навигации по связям.


## Как запустить тест?
1. Запустите `importer.py` для генерации данных и создания базы `links.db`.
2. Система создаст ~4000 уникальных связей для 1000 строк CSV.
3. Проверьте работоспособность, выполнив поиск по ID категории.


# LinkTheory: Ассоциативная Операционная Система (прототип)

Проект по реализации прикладной модели данных на основе "The Links Theory" ($L \to L^2$). 
Здесь данные хранятся не в иерархических папках, а в едином графе самореферентных связей и дуплетов.

## Текущий статус: Milestone 1 (Core & Shell)
Мы успешно реализовали ядро системы, механизм строгой дедупликации и семантический интерфейс управления.

### Ключевые возможности:
1. **Строгая дедупликация**: Одно атомарное значение (например, "Electronics") в системе всегда представлено **ровно одним** уникальным ID. Тысячи записей могут ссылаться на этот узел без дублирования данных.
2. **Семантическая навигация**: Переход по графу возможен как по числовым адресам (ID), так и по именам узлов.
3. **Многомерность**: Объекты могут принадлежать бесконечному количеству контекстов одновременно (один файл в разных "папках" без копирования).

## Структура проекта
- `core.py`: Ядро на SQLite. Реализует хранилище дуплетов $L \to L^2$ и таблицу значений.
- `importer.py`: Инструмент для "размола" CSV-массивов в атомарные связи.
- `shell.py`: Интерактивная оболочка для навигации по ассоциативному пространству.
- `query.py`: Скрипт для мгновенного поиска по графу.

## Как это работает (Демонстрация)

### 1. Подготовка данных
Удалите старую базу и импортируйте CSV (например, 1000 строк транзакций):
```bash
rm links.db
python importer.py

python shell.py

Примеры команд в Shell:

    cd Electronics — найти узел категории и сфокусироваться на нем.
    ls — увидеть все "щупальца" узла (например, все строки CSV, привязанные к категории).
    cd 3 — погрузиться внутрь конкретной строки данных.
    info — получить детальную информацию об узле (ID, значение, структура связи).
    back — вернуться на шаг назад по истории навигации.3. Пример многомерности (Тегирование)

Используйте tagger.py (в разработке), чтобы связать существующую строку данных с новым смыслом (например, "Favorites"). После этого объект будет доступен одновременно в двух точках входа: через категорию и через тег.

Что нужно сделать тебе:

    Запусти python tagger.py.
    Вернись в python shell.py.
    Введи cd Favorites.
    Введи ls.

Что ты должен увидеть:
Внутри Favorites ты увидишь row_0. Но если ты перейдешь в cd Electronics, ты тоже увидишь там row_0.
Это и есть магия: объект не «лежит» в папке. Он просто имеет связь и с тем, и с другим смыслом.